# React-SocketIO-Template

This is a basic react+node(MERN Stack) application with frontend setup using vite along with react + typescript + antd + tailwind.
SocketIO and express is used for server application.
